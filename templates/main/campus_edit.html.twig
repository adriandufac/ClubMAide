{% extends 'base.html.twig' %}

{% block title %}Ajouter un campus | {{ parent() }}{% endblock %}
{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/campus_edit.css') }}">
{% endblock %}

{% block body %}
    {{ form_start(campusform) }}
    {{ form_widget(campusform) }}
    <button>Edit</button>
    {{ form_end(campusform) }}
{% endblock %}